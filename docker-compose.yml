version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: g-stadtt-postgres
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Gismo
      POSTGRES_DB: gstadt
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
